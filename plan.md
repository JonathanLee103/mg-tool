# 订单表 & 库存表 生成方案

## 背景

将 4 张系统导出表通过字段映射和关联关系，生成两张输出表：**订单表**（19 字段）和**库存表**（32 字段）。

---

## 源文件与关键列

| 文件 | 行数 | 列数 | 关键列 |
|------|------|------|--------|
| 系统导出_采购订单数据.xlsx | 122 | 23 | 供应商订单子项号(主键), 规格描述, 牌号, 采购订货重量, 入库重量, 未交付重量, 表面质量, 最终用户名称, 股份终到站港名称, 运输方式名称, 采购交货月 |
| 系统导出_订单汇总.xls (Sheet0) | 3272 | 20 | 产销合同号(主键), 客户零件号, 后处理方式, 精整分流 |
| 系统导出_库存基本数据.xlsx | 4685 | 156 | 捆包号, 钢厂订单号, 规格, 牌号... |
| 系统导出_已发货库存明细.xlsx | 2725 | 126 | 同上（部分列名有差异） |

---

## 关联关系（已验证）

```
关联1: 采购订单数据.供应商订单子项号 = 订单汇总.产销合同号    (122/122 匹配, 100%)
关联2: 库存.钢厂订单号 = 采购订单数据.供应商订单子项号        (基本90/559, 已发货9/237, LEFT JOIN)
关联3: 库存.钢厂订单号 = 订单汇总.产销合同号                  (基本559/559, 已发货237/237, 100%)
关联4: 库存.钢厂订单号 = 订单汇总.SAP                        (基本559/559, 已发货237/237, 100%)
```

**说明**: 关联4 (钢厂订单号=SAP) 用于获取订货规格、客户零件号、宽度(套材判断)、后处理方式、精整分流。关联2 仅用于获取采购订货重量、表面质量、终到站港描述（匹配率低，LEFT JOIN，无匹配置空）。

---

## 处理流程

### Step 1: 加载数据
- 采购订单数据: `skiprows=1`（跳过英文表头，用中文表头）
- 订单汇总: 直接读取 Sheet0
- 库存基本数据 & 已发货库存明细: 直接读取

### Step 2: 库存 UNION
- 统一列名：`件数(张数,根数,支数)` → `件数`，已发货 `渠道规格描述` → `渠道规格`
- 已发货补缺失列：`库龄`、`原料最初入库日期(年月日)`、`业务入库日期(年月日)`
- 添加 `_source` 标记（basic / shipped）
- 取共同列，`pd.concat` 合并
- 识别捆包号重叠（同时存在于两个表）
- 计算规格（basic含重叠取渠道规格、shipped_only取规格）
- 计算首次入库时间（basic含重叠取原料最初入库日期、shipped_only取制造出厂发货时间）
- 计算最近入库日期（basic_only取业务入库日期、shipped含重叠取制造出厂发货时间）
- 日期格式化：YYYYMMDDHHMMSS → YYYY-MM-DD，YYYYMM → YYYY-MM

### Step 3: 构建订单表（122行 × 19列）

```
采购订单数据 
  ──LEFT JOIN──> 订单汇总 (on 供应商订单子项号 = 产销合同号)
  ──派生字段──> 厚度/直径, 宽度, 长度 (从 规格描述 按*拆分)
  ──字段映射──> 19列输出
```

### Step 4: 构建库存表（7410行 × 32列）

```
UNION库存 
  ──捆包号重叠检测──> 规格 (basic含重叠→渠道规格, shipped_only→规格)
              首次入库时间 (basic含重叠→原料最初入库日期, shipped_only→制造出厂发货时间)
              最近入库日期 (basic_only→业务入库日期, shipped含重叠→制造出厂发货时间)
  ──LEFT JOIN──> 采购订单数据 (on 钢厂订单号 = 供应商订单子项号, 获取采购订货重量/表面质量/终到站港描述)
  ──LEFT JOIN──> 订单汇总 (on 钢厂订单号 = SAP, 获取订货规格/客户零件号/宽度/后处理方式/精整分流)
  ──派生字段──> 厚度/直径, 宽度 (从 _规格 按*拆分), 长度=常量"C"
              套材 (存货性质=成品→"套材"; 存货性质=原料→按规格宽度与订单汇总.宽度比较, 一致→"非套材"/不一致→"套材")
              库龄 (制造出厂发货时间有值→当前日期-制造出厂发货时间; 无值→0)
              库龄(月) (库龄天数÷30, 商<10→"/m_0商", 商≥10→"/m_商")
              采购价格(不含税) (钢厂订单号=供应商订单子项号关联采购订单数据, 取子项不含税单价)
              品名(中文) (UNION.品名（中文）), 出厂车船号 (UNION.出厂车船号)
  ──日期格式化──> YYYYMMDDHHMMSS→YYYY-MM-DD, YYYYMM→YYYY-MM
  ──字段映射──> 32列输出
```

### Step 5: 输出 Excel

文件: `output_订单表_库存表.xlsx`，两个 Sheet（订单表、库存表）。

---

## 派生字段规则

| 字段 | 规则 | 示例 |
|------|------|------|
| 厚度/直径 | 规格字段按 `*` 拆分取第1个 | `0.65*1495*C` → `0.65` |
| 宽度 | 规格字段按 `*` 拆分取第2个 | `0.65*1495*C` → `1495` |
| 长度 | 固定值 `C` | `C` |
| 规格(库存表) | basic(含重叠)取渠道规格, shipped_only取规格 | 业务库存.渠道规格 或 已发货.规格 |
| 首次入库时间 | basic(含重叠)取原料最初入库日期(年月日), shipped_only取制造出厂发货时间 | — |
| 最近入库日期 | basic_only取业务入库日期(年月日), shipped(含重叠)取制造出厂发货时间 | — |
| 订货规格(库存表) | 通过 钢厂订单号=SAP 关联，直接取 订单汇总.订货规格 | `CA-DC56D+Z/0.8*1345/FB` |
| 套材(库存表) | 存货性质=成品→"套材"; 存货性质=原料→提取第二个*数字 vs 宽度, 一致→"非套材"/不一致→"套材" | 存货性质=成品→"套材"; `0.65*1470*1160`(原料)→1470 vs 1525→"套材" |
| 库龄(库存表) | 制造出厂发货时间有值→当前日期-制造出厂发货时间(先转YYYY-MM-DD格式); 无值→0 | `2025-11-13`→datetime.now()-2025-11-13=天数; 无值→0 |
| 库龄(月)(库存表) | 库龄天数÷30, 商<10→"/m_0商", 商≥10→"/m_商" | 15天→15÷30=0→"/m_00"; 365天→365÷30=12→"/m_12" |
| 日期格式 | YYYYMMDDHHMMSS→YYYY-MM-DD, YYYYMM→YYYY-MM, 已格式化则保持 | `20251113163622`→`2025-11-13`, `202509`→`2025-09` |

---

## 输出字段映射

### 订单表（19列，BASE = 采购订单数据）

| # | 输出字段 | 来源 |
|---|----------|------|
| 1 | 供应商合同号子项号 | 采购.供应商订单子项号 |
| 2 | 客户零件号 | 订单汇总.客户零件号 (LEFT JOIN) |
| 3 | 订货规格 | 采购.规格描述 |
| 4 | 订货牌号 | 采购.牌号 |
| 5 | 合同月份 | 采购.采购交货月 |
| 6 | 产销合同号 | 采购.供应商订单子项号 |
| 7 | 厚度/直径 | 派生: split 规格描述[0] |
| 8 | 宽度 | 派生: split 规格描述[1] |
| 9 | 长度 | 常量 "C" |
| 10 | 订货重量 | 采购.采购订货重量 |
| 11 | 表面质量 | 采购.表面质量 |
| 12 | 最终用户名称 | 采购.最终用户名称 |
| 13 | 终到站港描述 | 采购.股份终到站港名称 |
| 14 | 运输方式 | 采购.运输方式名称 |
| 15 | 后处理方式 | 订单汇总.后处理方式 |
| 16 | 精整分流 | 订单汇总.精整分流 |
| 17 | 订单量 | 采购.采购订货重量 (同#10) |
| 18 | 总明细量 | 采购.入库重量 |
| 19 | 未交付量 | 采购.未交付重量 |

### 库存表（36列，BASE = UNION库存 + 采购数据 + 订单汇总）

| # | 输出字段 | 来源 |
|---|----------|------|
| 0 | 资源号 | UNION.父捆包号材料管理号 |
| 1 | 捆包号 | UNION.捆包号 |
| 2 | 物料号 | UNION.物料号 |
| 3 | 母卷号 | UNION.母捆包号 |
| 4 | 规格 | basic(含重叠): 渠道规格; shipped_only: 规格 |
| 15 | 首次入库时间 | basic(含重叠): 原料最初入库日期(年月日); shipped_only: 制造出厂发货时间 |
| 16 | 最近入库日期 | basic_only: 业务入库日期(年月日); shipped(含重叠): 制造出厂发货时间 |
| 17 | 客户零件号 | 订单汇总.客户零件号 (LEFT JOIN via 钢厂订单号=SAP) |
| 18 | 订货规格 | 订单汇总.订货规格 (LEFT JOIN via 钢厂订单号=SAP) |
| 19 | 合同月份 | UNION.采购交货期 |
| 20 | 厚度/直径 | 派生: split UNION.规格[0] |
| 21 | 宽度 | 派生: split UNION.规格[1] |
| 22 | 长度 | 常量 "C" |
| 23 | 订货重量 | 采购.采购订货重量 (LEFT JOIN, 无匹配置空) |
| 24 | 表面质量 | 采购.表面质量 (LEFT JOIN) |
| 25 | 最终用户名称 | UNION.最终用户名称 |
| 26 | 终到站港描述 | 采购.股份终到站港名称 (LEFT JOIN) |
| 27 | 后处理方式 | 订单汇总.后处理方式 (LEFT JOIN via 钢厂订单号=SAP) |
| 28 | 精整分流 | 订单汇总.精整分流 (LEFT JOIN via 钢厂订单号=SAP) |
| 29 | 物资类别 | UNION.存货性质 |
| 30 | 套材 | 派生: 输出"套材"或"非套材" |
| 31 | 业务类型 | UNION.贸易方式 |
| 32 | 采购价格(不含税) | 采购.子项不含税单价 (LEFT JOIN via 钢厂订单号=供应商订单子项号, 无匹配置空) |
| 33 | 品名（中文） | UNION.品名（中文） |
| 34 | 出厂车船号 | UNION.出厂车船号 |
| 35 | 库龄（月） | 派生: 库龄÷30, 商<10→"/m_0商", 商≥10→"/m_商" |

---

## 待创建文件

- `/Users/lee/Project/MG/generate_tables.py` — 主脚本

## 输出文件

- `/Users/lee/Project/MG/output_订单表_库存表.xlsx`

---

## 验证计划

1. 订单表行数 = 采购订单数据行数 (122行)
2. 库存表行数 = 库存基本数据 + 已发货库存明细 (7410行)
3. 库存→订单汇总 SAP 匹配率 100%
4. 订货规格直接取自订单汇总.订货规格（非拼接）
5. 存货性质=成品 → 套材="套材"; 存货性质=原料 → 提取宽度与订单汇总.宽度比较, 一致→"非套材"/不一致→"套材"
6. 库龄: 制造出厂发货时间有值→当前日期-制造出厂发货时间(天数); 无值→0
7. 日期格式: YYYYMMDDHHMMSS→YYYY-MM-DD, YYYYMM→YYYY-MM
8. 规格: basic(含重叠)→渠道规格, shipped_only→规格
9. 首次入库时间: basic(含重叠)→原料最初入库日期, shipped_only→制造出厂发货时间
10. 最近入库日期: basic_only→业务入库日期, shipped(含重叠)→制造出厂发货时间
11. 采购价格(不含税): 钢厂订单号=供应商订单子项号关联采购订道数据, 匹配取子项不含税单价, 否则为空
12. 品名（中文）/出厂车船号: 从UNION库存直接取值
13. 库龄(月): 库龄天数÷30, 商<10→"/m_0商", 商≥10→"/m_商"
