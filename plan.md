# 订单表 & 库存表 生成方案

## 背景

将 4 张系统导出表通过字段映射和关联关系，生成两张输出表：**订单表**（19 字段）和**库存表**（32 字段）。

---

## 源文件与关键列

| 文件 | 行数 | 列数 | 关键列 |
|------|------|------|--------|
| 系统导出_采购订单数据.xlsx | 122 | 23 | 供应商订单子项号(主键), 规格描述, 牌号, 采购订货重量, 入库重量, 未交付重量, 表面质量, 最终用户名称, 股份终到站港名称, 运输方式名称, 采购交货月 |
| 系统导出_订单汇总.xls (Sheet0) | 3272 | 20 | 产销合同号(主键), 客户零件号, 后处理方式, 精整分流 |
| 系统导出_库存基本数据.xlsx | 4685 | 156 | 捆包号, 钢厂订单号, 规格, 牌号... |
| 系统导出_已发货库存明细.xlsx | 2725 | 126 | 同上（部分列名有差异） |

---

## 关联关系（已验证）

```
关联1: 采购订单数据.供应商订单子项号 = 订单汇总.产销合同号    (122/122 匹配, 100%)
关联2: 库存.钢厂订单号 = 采购订单数据.供应商订单子项号        (基本90/559, 已发货9/237, LEFT JOIN)
关联3: 库存.钢厂订单号 = 订单汇总.产销合同号                  (基本559/559, 已发货237/237, 100%)
```

**关键发现**: 关联3 是 100% 匹配，库存表可直接 JOIN 订单汇总，无需通过采购订单中转。

---

## 处理流程

### Step 1: 加载数据
- 采购订单数据: `skiprows=1`（跳过英文表头，用中文表头）
- 订单汇总: 直接读取 Sheet0
- 库存基本数据 & 已发货库存明细: 直接读取

### Step 2: 库存 UNION
- 统一列名差异：`件数(张数,根数,支数)` → `件数`
- 已发货补缺失列：`库龄`, `原料最初入库日期(年月日)`
- 日期列统一为字符串
- 取共同列 (120列)，`pd.concat` 合并
- 结果: 4685 + 2725 = 7410 行

### Step 3: 构建订单表（122行 × 19列）

```
采购订单数据 
  ──LEFT JOIN──> 订单汇总 (on 供应商订单子项号 = 产销合同号)
  ──派生字段──> 厚度/直径, 宽度, 长度 (从 规格描述 按*拆分)
  ──字段映射──> 19列输出
```

### Step 4: 构建库存表（7410行 × 32列）

```
UNION库存 
  ──LEFT JOIN──> 采购订单数据 (on 钢厂订单号 = 供应商订单子项号)
  ──LEFT JOIN──> 订单汇总 (on 钢厂订单号 = 产销合同号)
  ──派生字段──> 厚度/直径, 宽度, 长度 (从 规格 按*拆分)
              订货规格 (牌号 + 采购.规格描述 + 采购.表面质量)
              套材 (采购.规格描述 vs 库存.规格, 标准化比较)
  ──字段映射──> 32列输出
```

### Step 5: 输出 Excel

文件: `output_订单表_库存表.xlsx`，两个 Sheet（订单表、库存表）。

---

## 派生字段规则

| 字段 | 规则 | 示例 |
|------|------|------|
| 厚度/直径 | 规格字段按 `*` 拆分取第1个 | `0.65*1495*C` → `0.65` |
| 宽度 | 规格字段按 `*` 拆分取第2个 | `0.65*1495*C` → `1495` |
| 长度 | 固定值 `C` | `C` |
| 订货规格(库存表) | 牌号 + 采购.规格描述 + 采购.表面质量 (无分隔符) | `CA180/340YD+Z` + `0.65*1495*C` + `FD` = `CA180/340YD+Z0.65*1495*CFD` |
| 套材 | 采购.规格描述 ≠ 库存.规格 (数值标准化后比较) | `0.7*1680*C` vs `0.70*1680*C` → 非套材(标准化后一致) |

---

## 输出字段映射

### 订单表（19列，BASE = 采购订单数据）

| # | 输出字段 | 来源 |
|---|----------|------|
| 1 | 供应商合同号子项号 | 采购.供应商订单子项号 |
| 2 | 客户零件号 | 订单汇总.客户零件号 (LEFT JOIN) |
| 3 | 订货规格 | 采购.规格描述 |
| 4 | 订货牌号 | 采购.牌号 |
| 5 | 合同月份 | 采购.采购交货月 |
| 6 | 产销合同号 | 采购.供应商订单子项号 |
| 7 | 厚度/直径 | 派生: split 规格描述[0] |
| 8 | 宽度 | 派生: split 规格描述[1] |
| 9 | 长度 | 常量 "C" |
| 10 | 订货重量 | 采购.采购订货重量 |
| 11 | 表面质量 | 采购.表面质量 |
| 12 | 最终用户名称 | 采购.最终用户名称 |
| 13 | 终到站港描述 | 采购.股份终到站港名称 |
| 14 | 运输方式 | 采购.运输方式名称 |
| 15 | 后处理方式 | 订单汇总.后处理方式 |
| 16 | 精整分流 | 订单汇总.精整分流 |
| 17 | 订单量 | 采购.采购订货重量 (同#10) |
| 18 | 总明细量 | 采购.入库重量 |
| 19 | 未交付量 | 采购.未交付重量 |

### 库存表（32列，BASE = UNION库存 + 采购数据 + 订单汇总）

| # | 输出字段 | 来源 |
|---|----------|------|
| 0 | 资源号 | UNION.父捆包号材料管理号 |
| 1 | 捆包号 | UNION.捆包号 |
| 2 | 物料号 | UNION.物料号 |
| 3 | 母卷号 | UNION.母捆包号 |
| 4 | 规格 | UNION.规格 |
| 5 | 牌号 | UNION.牌号 |
| 6 | 入库重量 | UNION.净重(吨) |
| 7 | 入库数量 | UNION.件数 |
| 8 | 供应商合同号子项号 | UNION.钢厂订单号 |
| 9 | 客户名称 | UNION.客户名称 |
| 10 | 仓库名称 | UNION.仓库名称 |
| 11 | 捆包状态 | UNION.实物库存状态 |
| 12 | 封锁类型 | UNION.封锁类型 |
| 13 | 库龄 | UNION.库龄 (仅基本数据有值) |
| 14 | 品种代码 | UNION.品种附属码 |
| 15 | 首次入库时间 | UNION.原料最初入库日期(年月日) (仅基本数据有值) |
| 16 | 最近入库日期 | UNION.业务入库日期 |
| 17 | 客户零件号 | UNION.客户零件号 |
| 18 | 订货规格 | 派生: 牌号+采购.规格描述+采购.表面质量 |
| 19 | 合同月份 | UNION.采购交货期 |
| 20 | 厚度/直径 | 派生: split UNION.规格[0] |
| 21 | 宽度 | 派生: split UNION.规格[1] |
| 22 | 长度 | 常量 "C" |
| 23 | 订货重量 | 采购.采购订货重量 (LEFT JOIN, 无匹配置空) |
| 24 | 表面质量 | 采购.表面质量 (LEFT JOIN) |
| 25 | 最终用户名称 | UNION.最终用户名称 |
| 26 | 终到站港描述 | 采购.股份终到站港名称 (LEFT JOIN) |
| 27 | 后处理方式 | 订单汇总.后处理方式 (LEFT JOIN via 钢厂订单号=产销合同号) |
| 28 | 精整分流 | 订单汇总.精整分流 (LEFT JOIN) |
| 29 | 物资类别 | UNION.存货性质 |
| 30 | 套材 | 派生: 采购.规格描述 vs 库存.规格 |
| 31 | 业务类型 | UNION.贸易方式 |

---

## 待创建文件

- `/Users/lee/Project/MG/generate_tables.py` — 主脚本

## 输出文件

- `/Users/lee/Project/MG/output_订单表_库存表.xlsx`

---

## 验证计划

1. 订单表行数 = 采购订单数据行数 (122行)
2. 库存表行数 = 库存基本数据 + 已发货库存明细 (7410行)
3. 采购→订单汇总 JOIN 匹配率 100% (122/122)
4. 库存→订单汇总 JOIN 匹配率 100% (7410/7410)
5. 抽查：`0.65*1495*C` 拆分后 厚度=0.65, 宽度=1495, 长度=C
6. 抽查：套材字段对 `0.7*1680*C` vs `0.70*1680*C` 判断为 False（非套材）
